const Hero = () => {
  return (
    <section id="hero">
      <div className="hero-container">
        {/* If you want text here later, add it inside this div */}
      </div>
    </section>
  );
};

export default Hero;